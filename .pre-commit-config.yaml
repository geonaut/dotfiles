repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.2 # Use the latest version
    hooks:
      - id: gitleaks
        # This tells it to run during the 'push' phase
        stages: [pre-push]